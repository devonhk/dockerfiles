version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: ls -alh /var/run/docker.sock

      - run:
          name: Print the Current Time
          command: date